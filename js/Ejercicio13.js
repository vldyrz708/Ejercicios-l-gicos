let numero = prompt ( "Ingresa un número" )
if (numero % 2 == 0){
    console.log ("El número " + numero + " es número par.")
} else {
    console.log ("El número " + numero + " no es un númmero par.")
}